CREATE TABLE attributes (
    id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
    name TEXT,
    playCount INTEGER,
    lowResImage TEXT,
    highResImage TEXT
);

insert:
INSERT OR REPLACE INTO attributes(name, playCount, lowResImage, highResImage) VALUES (?, ?, ?, ?);

lastId:
SELECT last_insert_rowid();