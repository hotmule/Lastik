CREATE TABLE statistic (
    id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
    sectionId INTEGER,
    itemId INTEGER,
    rank INTEGER AS Int,
    playCount INTEGER
);

insert:
INSERT INTO statistic(sectionId, itemId, rank, playCount) VALUES (?, ?, ?, ?);

deleteSectionTop:
DELETE FROM statistic WHERE sectionId = ?;